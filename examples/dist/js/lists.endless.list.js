if (!window.EXAMPLES) {
    window.EXAMPLES = {};
}

const Examples = [{
    title: "List",
    props: [{}]
}];

window.EXAMPLES.UIEndlessList = {
    constructor: "Elements.Lists.UIEndlessList",
    list: Examples
};