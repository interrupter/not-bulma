import {
    addIndexField,
    createInnerTextNodeSnippet,
} from "/js/examples.helpers.js";

if (!window.EXAMPLES) {
    window.EXAMPLES = {};
}

const Examples = [
    {
        title: "UINavbarBrandWrapper",
        props: [{}],
    },
];

window.EXAMPLES.UINavbarBrandWrapper = {
    constructor: "Elements.Navigations.UINavbarBrandWrapper",
    list: Examples,
};
