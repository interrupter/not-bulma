if (!window.EXAMPLES) {
    window.EXAMPLES = {};
}

const Examples = [
    {
        title: "Simple",
        description: "Simple",
        props: [{}],
    },
];

window.EXAMPLES.UIProgress = {
    constructor: "Elements.Various.UIProgress",
    list: Examples,
};
